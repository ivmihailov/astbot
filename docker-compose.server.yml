services:
  astbot:
    image: astbot:0.1.0
    container_name: astbot
    restart: unless-stopped
    init: true
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./storage:/app/storage
    command: ["python", "-u", "bot.py"]
