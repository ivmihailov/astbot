[project]
name = "bott"
version = "0.1.0"
description = "Empty Python project scaffold"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"
